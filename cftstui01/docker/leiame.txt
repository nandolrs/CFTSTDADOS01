- Gerar build React

  npm run build ..\.

- Gerar imagem UI

  docker build -f ..\Dockerfile -t cftstui-image ..\build\.
  
  docker build -f ..\Dockerfile   --build-arg REACT_APP_SERVER_URL=http://host/caminho   -t cftstui-image  ..\build\.

  docker images
		
- Teste rápido
		
  docker run -it --rm -p 8000:80 --name cftstui-service cftstui-image

  docker run -it --rm -p 8000:80 --env REACT_APP_SERVER_URL=http://host/caminho1     --name cftstui-service-env cftstui-image

  acessar via navegador (chrome, edge, ...) = http://localhost:8000

  resposta = tela de pesquisa da aplicação

- Publicar no Docker
		
  docker tag cftstui-image nandolrs/cftstui-image

  docker image push nandolrs/cftstui-image

  acessar docker hub = https://hub.docker.com/r/nandolrs/cftstui-image

- Publicar no Docker v1r1

  docker tag cftstui-image nandolrs/cftstui-image-v1r1

  docker image push nandolrs/cftstui-image-v1r1

  acessar docker hub = https://hub.docker.com/r/nandolrs/cftstui-image-v1r1


-- node:14 AS build ;  node:13.12.0-alpine as build

  docker build -f Dockerfile -t cftstui-image .

  docker run -it --rm -p 8000:3000 --name cftstui-service cftstui-image

  docker run -it --rm -p 8000:3000 --env REACT_APP_SERVER_URL=http://host/caminho1     --name cftstui-service cftstui-image

  http://localhost:8000


--  node:13.12.0-alpine as build

  docker build -f ..\Dockerfile -t cftstui-image ..\.

    docker build -f ..\Dockerfile   --build-arg REACT_APP_SERVER_URL=http://host/caminho   -t cftstui-image  ..\.



  docker run -it --rm -p 8000:80 --name cftstui-service cftstui-image

  docker run -it --rm -p 8000:80 --env REACT_APP_SERVER_URL=http://host/caminho1     --name cftstui-service cftstui-image

  docker run -it --rm -p 8000:80 --env API_URL=http://host/caminho1     --name cftstui-service cftstui-image

  docker run -it --rm -p 8000:80 --env BASE_URL=http://host/caminho1     --name cftstui-service cftstui-image

  http://localhost:8000